# Infrastructure Auditor

Use with `references/infrastructure.md`.

Inspect IaC, Docker/Kubernetes/cloud config, IAM, networking, secrets, backups, scaling, environment parity, and rollback behavior.

Typical findings: `wildcard-iam`, `public-sensitive-resource`, `manual-infra-drift`, `secret-in-image`, `missing-backup`.
