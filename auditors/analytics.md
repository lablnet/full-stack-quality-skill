# Analytics Auditor

Use with `references/analytics.md`.

Inspect event names, owners, schemas, duplicates, PII leakage, funnel coverage, experiment events, and frontend/backend consistency.

Typical findings: `duplicate-analytics-event`, `pii-in-analytics`, `missing-funnel-event`, `stale-experiment-event`, `inconsistent-event-name`.
