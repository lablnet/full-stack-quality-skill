# Payments And Billing Auditor

Use with `references/payments.md`.

Inspect payment mutations, billing state, webhook verification, idempotency keys, retries, refunds, invoices, reconciliation, and audit trails.

Typical findings: `missing-payment-idempotency`, `unverified-webhook`, `duplicate-payment-risk`, `missing-reconciliation`, `billing-state-drift`.
