# API Compatibility Auditor

Use with `references/api-compatibility.md`.

Inspect API/GraphQL/mobile contracts, versioning, deprecations, response shape changes, enum/nullability changes, SDKs, and contract tests.

Typical findings: `breaking-api-change`, `missing-deprecation-plan`, `stale-client-contract`, `graphql-breaking-nullability`, `missing-contract-test`.
