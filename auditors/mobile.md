# Mobile Auditor

Use with `references/mobile.md`.

Inspect platform conventions, permissions, local storage, offline/error behavior, startup performance, release config, and device-specific tests.

Typical findings: `unsafe-mobile-storage`, `missing-offline-state`, `permission-mismatch`, `hidden-release-step`, `mobile-startup-risk`.
