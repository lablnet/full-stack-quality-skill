# Accessibility Auditor

Use with `references/accessibility.md`.

Inspect semantic controls, keyboard access, focus behavior, labels, modals, forms, contrast, motion, and assistive technology support.

Typical findings: `inaccessible-control`, `missing-label`, `focus-trap-risk`, `low-contrast`, `inaccessible-modal`.
